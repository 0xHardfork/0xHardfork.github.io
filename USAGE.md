# 动态目录加载使用指南

## 🎯 功能说明

首页现在会自动从 `_config.yml` 读取分类配置，并动态扫描 `pages/` 目录生成卡片和目录树，无需手动编辑首页代码。

## 📋 添加新分类

### 1. 在 `_data/categories.yml` 中添加分类配置

```yaml
- id: your-category-id        # 唯一标识，使用小写和连字符
  name: 分类名称                # 显示在卡片上的中文名称
  icon: 🔥                      # emoji图标
  description: 分类描述          # 一句话介绍
  path: pages/your-category     # 分类目录路径
```

**示例：添加"渗透测试"分类**

编辑 `_data/categories.yml`，在文件末尾添加：

```yaml
- id: penetration-testing
  name: 渗透测试
  icon: 🎯
  description: 网络渗透测试方法与工具
  path: pages/penetration-testing
```

### 2. 创建分类目录和文件

```bash
mkdir -p pages/penetration-testing
```

### 3. 创建分类首页

创建 `pages/penetration-testing/index.md`：

```markdown
---
layout: default
title: 渗透测试
---

# 渗透测试

网络渗透测试方法与工具。

## 内容目录

这里的内容会自动生成在首页卡片中。

---

[← 返回首页](/)
```

### 4. 添加内容页面

**一级页面**（直接在分类下）：
```bash
# pages/penetration-testing/web-pentesting.md
---
layout: default
title: Web渗透测试
---

# Web渗透测试

内容...

[← 返回渗透测试](./) | [← 返回首页](/)
```

**二级页面**（子目录中）：
```bash
mkdir -p pages/penetration-testing/tools
```

```markdown
# pages/penetration-testing/tools/index.md
---
layout: default
title: 渗透工具
---

# 渗透工具

常用渗透测试工具介绍。
```

```markdown
# pages/penetration-testing/tools/metasploit.md
---
layout: default
title: Metasploit
---

# Metasploit Framework

内容...

[← 返回渗透工具](./) | [← 返回首页](/)
```

## 🔄 目录树生成规则

系统会自动识别以下结构：

```
pages/your-category/
├── index.md              # 分类首页（不显示在目录中）
├── page1.md             # 一级页面 → 显示为 "- Page1"
├── page2.md             # 一级页面 → 显示为 "- Page2"
└── subfolder/           
    ├── index.md         # 子分类首页 → 显示为 "- Subfolder"
    ├── subpage1.md      # 二级页面 → 显示为 "  - Subpage1"
    └── subpage2.md      # 二级页面 → 显示为 "  - Subpage2"
```

**生成的目录树：**
```
云安全
  - 容器安全
    - Docker
Web安全
  - IAST
  - SAST
    - CodeQL
    - 大模型SAST
  - SAC
```

## ⚙️ 技术实现

### 配置文件
- `_data/categories.yml` - 分类元数据配置（独立数据文件）
- `_config.yml` - Jekyll站点配置

### 模板文件
- `index.md` - 首页，遍历categories生成卡片
- `_includes/category-tree.html` - 目录树生成组件

### Liquid逻辑
1. 读取 `site.categories` 配置
2. 对每个分类，扫描对应的 `path` 目录
3. 过滤出有 `title` 的页面（排除 index.md）
4. 按路径深度分组，构建两级目录树
5. 自动生成链接

## 📝 注意事项

### ✅ 必须做的
1. 每个页面必须包含 YAML front matter 和 `title`
2. 分类目录必须有 `index.md`
3. `_config.yml` 中的 `path` 必须准确

### ⚠️ 不要做的
1. 不要在首页手动添加卡片HTML
2. 不要忘记在新页面添加 `title`
3. 不要创建超过2级的深度（当前不支持）

### 🔧 修改样式
所有样式在 `_sass/jekyll-theme-hacker.scss` 中：
- `.category-card` - 卡片样式
- `.topic-list` - 目录列表样式

## 🚀 部署

修改 `_config.yml` 后需要重新部署：

```bash
git add .
git commit -m "feat: 添加新分类"
git push
```

GitHub Pages 会自动重新构建，几分钟后生效。

## 🎨 自定义emoji图标

可用的emoji参考：
- 🔐 🔒 🔓 - 安全相关
- ☁️ ⛅ 🌩️ - 云相关
- 🌐 🕸️ 💻 - Web相关
- 🎯 🔫 🗡️ - 攻击相关
- 🛡️ 🔰 ⚔️ - 防御相关
- 🔬 🧪 🔨 - 工具相关
- 📡 📶 🛰️ - 网络相关
- 🐳 📦 🎪 - 容器相关

## 🔍 Debug

如果目录没有正确生成：

1. 检查 `title` 是否设置
2. 检查 `path` 是否正确
3. 查看 Jekyll 构建日志
4. 确认文件位置是否在 `pages/` 下

---

**现在你只需要：**
1. 在 `_config.yml` 添加一行分类配置
2. 创建对应目录和markdown文件
3. Push到GitHub

首页会自动生成新的卡片！🎉
